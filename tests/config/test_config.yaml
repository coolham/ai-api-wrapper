# 测试配置文件
http:
  timeout: 50.0
  max_retries: 5

logging:
  level: "DEBUG"

providers:
  test_provider:
    base_url: "https://api.test.com"
    models:
      - "test-model-1"
      - "test-model-2"
    default_model: "test-model-1"
    use_proxy: true 